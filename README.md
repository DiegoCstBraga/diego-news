# Diego news

Projeto utilizado nas aulas do `curso.dev`

## Run docker

```bash
# -f = --file (specify where the compose.yaml file is located)
# -d = --detach (run container and set the terminal "available")
docker compose -f infra/compose.yaml up
```
